import { Container } from "react-bootstrap";

export const UserList = () => {
    return (
        <Container>Main user list</Container>
        /* 
        TODO: get all users from DB
        TODO: create small card of basic info about each user. Something like, picture, fName, lName, and one or two other things
        TODO: make each card clickable 
        TODO: when each card is clicked then it will direct the user to the users profile page
        TODO: add button in NavBar to take current user to their own profile page to view, edit, whatevs 
        */
    )
}